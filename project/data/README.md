# Data

This repository should use synthetic, public, licensed or appropriately
de-identified data only.

Suggested structure:

```text
data/
├── sample/
├── processed/
├── metadata/
└── private/   # ignored by git
```

Record the source, license, preprocessing and intended use for every dataset.
